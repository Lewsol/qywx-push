# 项目问题清单与修复建议

## 1. 文档目的

本文记录当前代码库在初始化和代码审查过程中发现的安全、可靠性、部署、依赖和可维护性问题，并给出建议的修复顺序、实现方向和验收标准。

本文是问题分析和整改建议，不表示这些问题已经修复。实施修复时应根据部署环境、现有 SQLite 数据和前端兼容要求拆分提交，并在每个阶段完成回归验证。

## 2. 项目现状摘要

项目是一个基于 Node.js、Express 和 SQLite 的企业微信通知转发服务，支持：

- 配置企业微信 CorpID、CorpSecret、AgentID 和接收成员
- 验证企业微信凭证并拉取成员列表
- 使用唯一 `code` 调用通知接口
- 企业微信回调 URL 验证
- 加密 XML 回调消息接收和解析
- 使用 SQLite 保存配置

当前仓库没有自动化测试、lint、format 或 CI 配置。依赖安装和基础启动可以成功完成，但存在若干生产环境风险。

## 3. 优先级定义

| 优先级 | 含义 |
| --- | --- |
| P0 | 公网或生产部署前应完成，涉及凭证泄漏、配置接管、镜像泄漏或严重依赖漏洞 |
| P1 | 建议近期完成，涉及启动可靠性、配置一致性、接口滥用和运维风险 |
| P2 | 可维护性改进，应在核心风险修复后逐步完成 |

## 4. 问题总览

| 编号 | 优先级 | 问题 | 主要影响 |
| --- | --- | --- | --- |
| SEC-001 | P0 | 缺少 `.dockerignore` | 密钥、数据库和本机依赖可能进入镜像 |
| SEC-002 | P0 | 存在默认加密密钥，缺少密钥生命周期管理 | 凭证可被解密，换钥后旧数据不可用 |
| SEC-003 | P0 | 配置管理入口没有认证，且旧接口可返回已有 `code` | 无需预先持有 `code` 也可能恢复 bearer credential |
| SEC-004 | P0 | `callback_token` 明文存储并通过接口返回 | 回调验证凭证泄漏 |
| SEC-006 | P0 | 当前部署方式没有强制 HTTPS | 管理凭证和回调密钥可能被明文截获 |
| SEC-007 | P0 | 敏感表单加载第三方脚本且存在 DOM XSS 注入点 | 前端供应链或注入可窃取凭证 |
| SEC-008 | P0 | 日志记录完整 `code` 和回调消息正文 | bearer credential 和企业内部消息泄漏 |
| DEP-001 | P0 | npm 审计发现 22 个漏洞，运行时仍固定 Node.js 18 | 包含 3 个 critical、12 个 high，且阻碍部分升级 |
| REL-001 | P1 | 数据库 schema 初始化和端口监听存在竞态 | 端口可能在数据库完全就绪前开放 |
| SEC-005 | P1 | 通知和配置接口没有限流 | 消息滥发、撞库和资源消耗风险 |
| NET-001 | P1 | 企业微信出站 HTTP 请求没有超时和总截止时间 | 上游挂起会长期占用连接和内存 |
| DB-001 | P1 | 两步配置占位值与数据库唯一约束冲突 | 中断、重试或并发配置可能持续失败 |
| VAL-001 | P1 | 请求参数校验较弱 | 非法数据、超长内容和异常状态写入数据库 |
| CFG-001 | P1 | `WECHAT_API_BASE` 环境变量没有生效 | 配置与实际行为不一致，不利于测试 |
| OPS-001 | P1 | 应用、Dockerfile 和 Compose 端口语义不一致 | 独立容器部署容易配置错误 |
| API-001 | 待确认 | 回调错误响应格式不完全一致 | 需先对照企业微信协议判断是否为缺陷 |
| TEST-001 | P2 | 缺少自动化测试 | 安全和迁移改动缺少回归保障 |
| ARCH-001 | P2 | 路由层存在业务逻辑和无用实例 | 分层不清，后续扩展成本增加 |
| DEP-002 | P2 | 依赖治理和升级策略不足 | 旧依赖长期积累，升级风险上升 |
| DOC-001 | P2 | README 主要是 Docker 部署说明 | 本地开发、安全边界和 API 约束不完整 |

---

## 5. P0：生产部署前应修复

### SEC-001：缺少 `.dockerignore`

**修复状态：✅ 已修复**

已新增 `.dockerignore`，排除环境文件、数据库、日志、Git 元数据、本机依赖和常见临时文件，避免它们进入 Docker 构建上下文。

#### 现状

- 仓库没有 `.dockerignore`。
- `Dockerfile` 和 `Dockerfile.alternative` 都执行 `COPY . .`。
- `.gitignore` 不会自动限制 Docker 构建上下文。

#### 风险

构建镜像时，下列内容可能被发送到 Docker daemon 或写入镜像层：

- `.env` 和其他环境配置
- `database/notifier.db`
- 日志文件
- `.git` 历史和元数据
- 本机 `node_modules`
- IDE、缓存和临时文件

如果 `.env` 中包含 `ENCRYPTION_KEY`，同时数据库也进入镜像，攻击者可能同时获得密文和解密密钥。本机 `node_modules` 还可能覆盖容器内安装的 Linux 原生依赖，造成 `sqlite3` 平台不兼容。

#### 修复建议

新增 `.dockerignore`，至少包含：

```dockerignore
.git
.gitignore
.env
.env.*
node_modules
npm-debug.log*
database
logs
*.log
.tmp
.cache
.vscode
.idea
.DS_Store
Thumbs.db
```

生产密钥通过运行环境、Compose secret、Docker secret 或部署平台的 secret 管理功能注入，不得复制进镜像。

#### 验收标准

- Docker 构建上下文不包含 `.env`、数据库、日志、`.git` 或本机 `node_modules`。
- 构建后的镜像中不存在真实凭证和历史数据库。
- 容器内 `sqlite3` 可以正常加载并创建数据库。

---

### SEC-002：默认加密密钥和密钥生命周期问题

**修复状态：✅ 已修复**

已移除运行时和部署配置中的默认密钥，启动时强制校验 `ENCRYPTION_KEY` 为恰好 32 字节的可打印 ASCII；`CryptoService` 不再静默补齐或截断，并提供符合约束的高熵密钥生成器。新增密钥轮换脚本，在自动创建 SQLite 备份后，通过事务使用旧密钥解密 CorpSecret 和 EncodingAESKey、校验企业微信字段格式，再以新密钥重加密，任一失败即回滚；旧版补齐/截断语义仅保留在迁移脚本中。轮换备份被强制写入项目目录之外，并增加 Git/Docker 忽略规则作为额外防线；POSIX 权限会收紧为 `0600`，Windows 则要求预先确认受限 ACL。README 已补充密钥生成、注入、备份、轮换和恢复流程。

#### 原现状

- `src/services/notifier.js` 和 `src/api/routes.js` 在未配置时使用开发默认密钥。
- `docker-compose.yml` 包含公开的示例密钥。
- `src/core/crypto.js` 将输入字符串补齐或截断到前 32 个字符后直接用于 AES-256-CBC。
- 当前没有密钥版本、密钥标识或自动轮换机制。

#### 风险

- 生产环境误用默认密钥时，数据库中的 CorpSecret 和 EncodingAESKey 可以被轻易解密。
- 已有数据写入后直接修改 `ENCRYPTION_KEY`，会导致旧数据永久无法解密。
- 非 ASCII 字符可能产生不符合预期的字节长度。
- 当前 `CryptoService.generateKey()` 返回 64 位十六进制字符串，但构造函数只取前 32 个字符，行为容易造成误解。

#### 修复建议

第一阶段：

1. 移除代码中的生产可用默认值。
2. 启动时校验 `ENCRYPTION_KEY`；生产环境缺少或格式错误时立即退出。
3. 明确要求高熵、稳定、经过安全备份的 32 字节 ASCII 密钥。
4. 从 Compose 文件中移除硬编码示例密钥，改为 `${ENCRYPTION_KEY}` 或 secret 注入。

第二阶段：

1. 为密文增加版本或密钥 ID。
2. 实现显式轮换流程：旧密钥解密，再使用新密钥重加密。
3. 轮换前备份数据库，并提供失败回滚方式。
4. 评估使用经过认证的加密模式，例如 AES-256-GCM，以同时提供机密性和完整性保护。

#### 验收标准

- 生产环境未设置有效密钥时，服务拒绝启动并输出不含敏感信息的明确错误。
- 示例配置不包含可直接用于生产的固定密钥。
- 修改密钥前有可执行的数据迁移和回滚步骤。
- 新旧数据都可在迁移测试中正确解密。

---

### SEC-003：配置管理入口无认证，且旧接口可泄漏已有 `code`

**修复状态：✅ 已修复**

已新增运行时必填且至少 32 字符的 base64url 安全随机 `ADMIN_TOKEN`，所有配置管理接口在读取请求体前统一使用常量时间比较的 Bearer 认证，未认证请求统一返回 401。数据库在写锁事务中兼容迁移独立 `notify_token`：旧记录以原 `code` 初始化以保持旧通知 URL 可用，升级流程明确要求停止全部无认证的旧实例后再迁移；新配置使用稳定 callback/config code 和独立随机通知 token，通知查询仅按通知 token，管理和 callback 继续按稳定 code。新增管理员通知 token 轮换接口，轮换后旧通知 URL 失效且 callback URL 不变。重复配置和重复回调配置统一返回不含既有凭证的 409。前端管理员 Token 仅保存在页面 JavaScript 内存，所有管理请求经统一认证封装；部署模板和文档已同步权限边界、迁移兼容与轮换方式。配置 code 和通知 token 的运行日志已改为不可逆摘要。

#### 原现状

服务没有独立的认证和授权中间件。`code` 同时用于：

- `POST /api/notify/:code`：发送消息
- `GET /api/configuration/:code`：读取配置
- `PUT /api/configuration/:code`：修改配置
- `GET/POST /api/callback/:code`：企业微信回调

风险不仅是“已有 `code` 泄漏”。旧兼容接口 `POST /api/configure` 也没有认证，其重复配置判断只使用 `corpid + agentid + touser + callback_enabled + callback_token`，不验证请求中的 CorpSecret 是否真实。匹配到已有配置后会直接返回现有完整 `code`。攻击者只要猜中或知道相关非密钥字段，并提供任意非空 CorpSecret，就可能恢复现有 bearer credential。

以下配置管理入口目前均没有管理员认证：

- `POST /api/validate`
- `POST /api/generate-callback`
- `POST /api/complete-config`
- `POST /api/configure`
- `GET /api/configuration/:code`
- `PUT /api/configuration/:code`

#### 风险

通知 URL 经常会进入脚本、日志、聊天记录或监控系统。通知调用方不应自然获得配置管理权限。当前设计可能导致：

- 无需预先持有 `code` 即可通过重复配置路径获取旧 `code`
- 消息滥发
- 接收人或 CorpSecret 被替换
- 回调配置被接管
- 原有通知和回调链路被破坏
- 对 `/api/configure` 持续枚举

#### 修复建议

1. 将所有配置创建、验证、完善、查询和更新入口统一纳入管理员认证。
2. 查重时不得向未认证请求返回已有 bearer token；可返回 409，或只允许管理员认证后执行恢复操作。
3. 拆分三类标识和权限：
   - 通知发送 token；
   - 管理员身份或管理 token；
   - 企业微信回调 URL 使用的稳定、低权限标识。
4. 通知 token 只能发送消息，不能查询或修改配置。
5. 日志只记录不可逆摘要或脱敏标识，不记录完整 token。
6. 前端、API 文档、错误响应和截图中不得暴露完整有效令牌。
7. 长期建议存储发送 token 的哈希，并提供吊销、轮换和审计能力。

#### 兼容性说明

这是接口安全模型调整，可能影响：

- 现有前端配置流程；
- 通知调用脚本；
- 已配置到企业微信后台的回调 URL。

回调 URL 需要稳定性，迁移时应保留旧 URL 兼容窗口，或建立旧标识到新内部配置的安全映射，不能只替换 URL 后立即失效。

#### 验收标准

- 预置配置后，使用相同 `corpid/agentid/touser` 和错误 CorpSecret 调用 `/api/configure`，响应不得包含已有 `code`。
- 所有未认证的配置管理入口返回 401/403。
- 仅持有通知 token 的调用者无法读取或修改配置。
- 旧回调 URL 在约定迁移窗口内仍按设计工作。
- 日志和错误响应不输出完整 token。
- token 可以轮换或吊销。

---

### SEC-004：`callback_token` 明文存储并返回

#### 现状

- `callback_token` 以明文保存到 SQLite。
- `GET /api/configuration/:code` 会返回 `callback_token`。
- 代码注释将该响应描述为不包含敏感信息，与实际行为不一致。

#### 风险

回调 Token 用于企业微信消息签名校验，应视为敏感配置。数据库、配置接口或 `code` 泄漏时，Token 也会被泄漏。

#### 修复建议

1. 立即停止通过配置查询接口返回完整 `callback_token`。
2. 前端仅展示“已配置”状态；修改时要求重新输入，不能回显原值。
3. 使用随机 IV 的认证加密保存 Token 密文，供回调校验时解密。
4. 当前业务会按 Token 做重复配置等值查找，随机化密文不能直接用于等值查询。应额外保存 keyed HMAC/查找摘要并建立索引，或按其他字段查询有限候选后逐条解密比较；不建议使用确定性加密代替查找摘要。
5. 数据库迁移应支持双读阶段：
   - 保留旧明文字段用于短期兼容；
   - 增加密文、密文版本和查找摘要字段；
   - 迁移旧记录并验证；
   - 新旧代码兼容窗口结束后再清理明文字段。
6. 更新接口使用“未提供则保持原值”的语义，避免前端留空导致 Token 被清除。

#### 验收标准

- 配置查询接口不返回完整回调 Token。
- 新写入数据库的 Token 不是明文。
- 相同 Token 的重复创建语义在迁移前后保持一致，不受随机 IV 影响。
- 旧配置和新配置都能完成 URL 验证和消息回调。
- 日志不输出 Token。

---

### SEC-006：公网传输没有强制 HTTPS

#### 现状

应用仅启动普通 HTTP 服务，Compose 将端口直接映射到宿主机，README 也使用 `http://` 示例。管理页面会提交 CorpSecret、callback token 和 EncodingAESKey。

#### 风险

如果直接暴露公网或不可信网络，链路中的攻击者可能窃听或篡改管理凭证、通知 token 和回调密钥。仅增加应用层认证不能解决明文传输问题。

#### 修复建议

1. 在可信反向代理、Ingress 或部署平台终止 TLS。
2. 公网只开放 443；应用端口只绑定环回地址、私有网络或 Docker 内部网络。
3. HTTP 入口只允许跳转到 HTTPS，不应接收包含敏感字段的 API 请求。
4. API 文档、前端生成的通知 URL 和回调 URL 默认使用 HTTPS。
5. 如果根据 `X-Forwarded-Proto` 判断协议，必须正确配置可信代理，不能无条件信任客户端请求头。
6. 配置合理的 TLS 版本、证书自动续期和 HSTS；首次确认部署无 HTTP 依赖后再启用长期 HSTS。

#### 验收标准

- 公网只开放 443，应用内部端口不能被公网直接访问。
- HTTP 页面和安全方法请求按部署策略跳转；明文敏感 API 请求不被处理。
- 浏览器和 API 客户端提交的 CorpSecret、callback token、EncodingAESKey 全程通过 TLS。
- 证书校验、续期和反向代理配置经过验证。

---

### SEC-007：第三方前端脚本和 DOM XSS 风险

#### 现状

配置页面加载 Tailwind、DaisyUI、Lucide 和 GSAP 的远程资源，其中 Lucide 使用未固定版本的 `@latest`。这些脚本在包含 CorpSecret、callback token 和 EncodingAESKey 的页面上下文中执行。

`public/script.js` 多处将成员名称、UserID、描述、错误消息和接口返回值直接拼接到 `innerHTML`，缺少上下文转义。

#### 风险

- 第三方 CDN 或上游包被攻陷时，脚本可读取表单内的全部敏感字段。
- 恶意成员名称、描述或错误消息可能触发 DOM XSS。
- XSS 可窃取管理员凭证、配置 `code` 和企业微信密钥。

#### 修复建议

1. 生产环境优先自托管前端依赖。
2. 如果必须使用 CDN，固定精确版本并使用 Subresource Integrity；移除 `@latest`。
3. 使用 `textContent`、`createElement` 和 DOM API 构造动态内容，不直接拼接不可信值到 `innerHTML`。
4. 设置严格 CSP，限制脚本、样式、连接和 frame 来源，并逐步移除不必要的内联脚本/样式要求。
5. 对 URL、属性值和文本节点分别使用正确的上下文处理方式。

#### 验收标准

- 使用包含 `<img src=x onerror=...>`、引号和标签字符的成员名、描述及错误消息时不会执行脚本。
- 页面不加载未固定版本的第三方脚本。
- CSP 不允许任意远程脚本执行。
- 浏览器开发者工具和构建产物中不存在真实密钥泄漏。

---

### SEC-008：日志泄漏 `code` 和回调正文

#### 现状

服务层在创建、查重、完善配置和处理回调时会记录完整 `code`。文本回调会记录发送者和完整消息正文，路由层还会再次输出解析后的消息对象。

#### 风险

容器日志通常会被持久化或汇聚到集中日志系统。当前日志可能同时泄漏：

- 可用于发送、查询和修改配置的完整 bearer `code`
- 企业内部用户标识
- 企业内部消息正文
- 回调处理结果中的其他业务数据

#### 修复建议

1. 默认只记录不可逆请求标识、消息类型、状态和耗时。
2. 完整 `code` 使用哈希摘要或固定脱敏格式替代。
3. 禁止记录 CorpSecret、EncodingAESKey、callback token 和回调正文。
4. 调试日志必须显式启用、限时使用，并执行字段级脱敏。
5. 对日志平台设置最小权限、保留期限和访问审计。

#### 验收标准

- 捕获测试 logger 后，输出中不包含完整 `code`、CorpSecret、EncodingAESKey、callback token 或消息正文。
- 正常日志仍能通过请求 ID、配置摘要和错误类别定位问题。

---

### DEP-001：npm 依赖漏洞和过期运行时

#### 当前审计结果

当前锁文件安装后，`npm audit` 报告：

| 严重级别 | 数量 |
| --- | ---: |
| Critical | 3 |
| High | 12 |
| Moderate | 5 |
| Low | 2 |
| 合计 | 22 |

涉及的直接依赖包括：

| 依赖 | 当前安装版本 | 审计级别 | 说明 |
| --- | --- | --- | --- |
| `axios` | 1.10.0 | High | 有可用修复版本 |
| `express` | 4.21.2 | High | 有可用修复版本 |
| `sqlite3` | 5.1.7 | High | npm 建议升级到 6.x，属于主版本升级 |
| `uuid` | 9.0.1 | Moderate | npm 建议升级到新主版本 |
| `xmldom` | 0.6.0 | Critical | npm 报告无可用修复版本 |

部分间接依赖漏洞来自 `sqlite3` 的构建依赖链，包括 `tar`、`node-gyp`、`cacache` 和 `make-fetch-happen` 等。

两个 Dockerfile 当前固定在已停止维护的 Node.js 18 大版本。npm 当前建议的 `sqlite3@6.0.1` 要求 Node.js `>=20.17.0`，继续停留在 Node.js 18 会阻碍该依赖链的修复。

#### 风险

漏洞是否可被远程利用取决于实际执行路径，但 Critical/High 不能仅因为是间接依赖而忽略。尤其需要关注处理外部 HTTP、URL、表单和 XML 内容的运行时依赖。停止维护的运行时本身也无法持续获得安全更新。

#### 修复建议

不要直接执行 `npm audit fix --force`。建议在独立分支中分批处理：

1. 先将运行时升级到当前仍受支持的 Node.js LTS，并确保版本满足 `sqlite3@6` 的最低要求；固定具体补丁版本或镜像 digest，而不是继续固定 Node.js 18。
2. 在升级后的运行时上执行普通 `npm audit fix`，审查 `package-lock.json` 变化并运行回归测试。
3. 显式升级可在当前主版本内修复的 `axios` 和 `express`。
4. 检查源码没有直接使用 `xmldom` 后，尝试移除该直接依赖：
   ```bash
   npm uninstall xmldom
   ```
   随后重点验证企业微信回调 XML 解析，因为实际代码使用 `wxcrypt` 导出的 `x2o`。
5. 在受支持的 Node.js LTS 上单独评估 `sqlite3` 6.x：
   - Windows/Linux 原生模块构建；
   - Docker 构建；
   - SQLite 文件兼容性；
   - 现有 CRUD 和唯一约束行为。
6. 单独评估 `uuid` 主版本升级；如果新版本转为 ESM，应处理当前 CommonJS 兼容问题，或改用 Node.js 原生 `crypto.randomUUID()`。
7. 每次升级后重新执行：
   ```bash
   npm install
   npm audit
   node --check server.js
   npm start
   ```
8. 不应仅为了降低审计数字而接受无法解释的破坏性升级。

#### 验收标准

- 镜像内 `node --version` 为当前受支持的 LTS，且满足所有生产依赖的 engine 要求。
- Critical 漏洞清零。
- High 漏洞已修复，或有明确的不可利用分析、补偿措施和跟踪计划。
- 服务可启动，SQLite 可加载，通知和回调流程通过回归验证。
- `package.json` 和 `package-lock.json` 保持一致。

---

## 6. P1：建议近期修复

### REL-001：数据库初始化竞态

#### 现状

`src/services/notifier.js` 在模块加载时调用：

```js
db.init().catch(console.error);
```

该 Promise 没有被应用启动流程等待。`server.js` 可能在数据库准备完成前开始监听端口。

#### 风险

`sqlite3.Database` 对象通常会在 `db.init()` 调用期间同步赋值，真正没有被等待的是数据库连接打开、建表完成和整体就绪状态。因此主要风险是：

- 端口可能在数据库连接和 schema 初始化成功前开放。
- 早期查询可能与建表操作并发或排队，行为依赖 sqlite3 调度。
- 数据库初始化失败时，服务仍可能继续监听并呈现“已启动”状态。
- 当前没有健康检查，容器“运行中”不能代表应用和数据库已经就绪。

#### 修复建议

1. 将数据库初始化从模块副作用改为显式启动步骤。
2. 数据库成功初始化后再调用 `app.listen()`。
3. 初始化失败时记录错误并以非零状态退出进程。
4. 增加 `/health` 或 `/ready`，区分进程存活和依赖就绪。
5. 增加优雅关闭，在 `SIGTERM`/`SIGINT` 时关闭 HTTP server 和数据库连接。

#### 验收标准

- 数据库初始化完成前端口不接受业务请求。
- 数据库路径不可写或建表失败时，进程以非零状态退出。
- 健康检查能够反映数据库就绪状态。

---

### SEC-005：缺少限流和滥用防护

#### 现状

通知、凭证验证、配置更新和回调接口均没有限流。

#### 风险

- 泄漏的 `code` 可被用于批量发送消息。
- `/api/validate` 可持续调用企业微信 API，消耗配额并产生负载。
- 大量无效回调请求会消耗 XML 解析和加解密资源。

#### 修复建议

- 在应用层或反向代理层增加基于 IP、token 和路由的限流。
- 通知接口设置合理的每分钟和突发请求上限。
- 管理接口设置更严格的限流和失败惩罚。
- Express 当前使用默认约 100 KB 的 body 限制，但没有显式、按路由配置；应根据通知、管理和回调接口分别设置明确上限，并验证超限返回 413。
- 记录脱敏的限流事件，避免记录完整凭证。

#### 验收标准

- 超过阈值时返回 429。
- 单个 token 的滥用不会影响全部配置。
- 合法企业微信回调不会因统一限流策略被误伤。

---

### NET-001：企业微信出站请求没有超时和总截止时间

#### 现状

`src/core/wechat.js` 中获取 token、发送消息、查询部门和查询成员的 Axios 请求都没有显式超时、响应大小限制或取消信号。`getAllUsers()` 还会顺序遍历所有部门。

#### 风险

- 上游连接半开、代理异常或企业微信 API 长时间不返回时，请求可能长期挂起。
- 挂起请求持续占用 HTTP 连接、内存和应用并发能力。
- `/api/validate` 的总耗时会随部门数量累积；仅给单次请求加超时仍不能限制整个流程。
- 入口限流不能主动释放已经挂起的出站请求。

#### 修复建议

1. 创建统一 Axios 实例，配置连接/总请求超时和合理的响应大小限制。
2. 使用 `AbortController` 或等价机制给每个请求设置取消信号。
3. 给整个 `/api/validate` 流程设置总截止时间。
4. 对部门遍历设置有限并发，而不是无限并发或完全串行。
5. 只对安全且幂等的请求进行有限重试，并使用指数退避和抖动。
6. 将上游超时、连接失败和业务错误分别映射为明确的 502/504 或合适错误响应。

#### 验收标准

- Mock 企业微信端保持连接但不返回时，本地 API 在规定的 N 秒内结束。
- 大量并发超时后，活动连接数和内存可以恢复。
- 总截止时间能中止尚未完成的部门遍历。
- 发送消息接口不会因不安全重试导致重复发送。

---

### DB-001：两步配置占位值与唯一约束冲突

#### 现状

`configurations` 表包含 `UNIQUE(corpid, agentid, touser)`。两步配置第一阶段保存回调信息时统一写入：

- `agentid = 0`
- `touser = ''`

因此，同一 CorpID 已存在一条未完成草稿后，如果使用不同 callback token 再次生成回调配置，业务查重可能无法命中，但数据库唯一约束会冲突并返回 500。

#### 风险

- 用户中途关闭浏览器后可能无法重新开始配置。
- 并发创建或使用不同 Token 重试可能留下不可恢复草稿。
- 数据库约束错误被当作通用服务器错误，不利于用户处理。

#### 修复建议

1. 为配置增加明确状态，例如 `draft`、`active`、`disabled`。
2. 重新设计唯一约束，使草稿配置不会因为统一占位值相互冲突。
3. 支持经认证的草稿恢复、替换和过期清理。
4. 将已知唯一约束冲突映射为 409，而不是 500。
5. 设计迁移时同时考虑已有完整配置和未完成草稿。

#### 验收标准

- 同一 CorpID 的中断重试可以按产品规则恢复或重新创建。
- 并发创建不会生成不可解释的 500。
- 多个合法企业微信应用配置不会被占位字段错误阻止。
- 过期草稿可安全清理，不影响活动配置。

---

### VAL-001：参数校验不足

#### 现状

当前主要通过字段是否存在进行判断，缺少统一 schema 校验。部分字段没有严格检查：

- `agentid` 类型和范围
- `touser` 数组元素格式及数量
- 标题和正文长度
- `code` 格式
- CorpID、Token 等字符串长度
- 更新接口允许修改的字段集合

#### 风险

- 异常或超长数据进入数据库。
- 消息发送时才暴露错误，用户难以定位。
- 当前 body parser 有默认约 100 KB 限制，但限制未显式记录、未按路由区分，也缺少 413 行为测试。
- 更新接口出现意外字段覆盖或状态不一致。

#### 修复建议

- 引入轻量 schema 校验库，或建立统一的显式校验函数。
- 显式配置 JSON 和原始 XML body 上限，并按路由区分；不要依赖框架默认值。
- 对创建和更新接口采用字段白名单。
- 明确区分“字段未提供”和“字段希望清空”。
- 将企业微信约束转换为本地可读错误。

#### 验收标准

- 非法输入在进入 service/database 前返回 400。
- 响应不包含密钥或底层异常堆栈。
- 边界值和空值行为有测试覆盖。

---

### CFG-001：`WECHAT_API_BASE` 没有生效

#### 现状

`env.template` 和 Compose 定义了 `WECHAT_API_BASE`，但 `routes.js` 和 `notifier.js` 都直接执行：

```js
new WeChatService()
```

因此仍使用类中的硬编码默认地址。

#### 风险

- 配置项会误导运维人员。
- 无法方便地接入 mock server 或代理。
- 路由层和服务层可能在后续修改中使用不同地址。

#### 修复建议

统一创建实例：

```js
const apiBase = process.env.WECHAT_API_BASE || 'https://qyapi.weixin.qq.com';
const wechat = new WeChatService(apiBase);
```

更好的方案是通过应用启动模块创建实例并注入 service/router，减少重复实例和配置漂移。

如果项目明确不支持自定义地址，则删除模板和 Compose 中的该变量。

#### 验收标准

- 设置 `WECHAT_API_BASE` 后，所有企业微信请求都使用该地址。
- 未设置时仍使用官方地址。
- 自动化测试可使用本地 mock server。

---

### OPS-001：端口配置不一致

#### 现状

- `server.js` 未设置 `PORT` 时监听 3000。
- `docker-compose.yml` 设置并映射 12121。
- 默认 `Dockerfile` 声明 `EXPOSE 12121`，但没有设置 `PORT`。
- `Dockerfile.alternative` 声明 `EXPOSE 3000`。

`EXPOSE` 只是镜像元数据，不会改变 Node.js 实际监听端口。因此独立运行默认镜像且不传 `PORT` 时，应用仍监听 3000。

#### 修复建议

选择一个统一策略：

- 推荐应用和镜像默认使用 3000，Compose 映射 `12121:3000`；或
- 在默认 Dockerfile 中显式设置 `ENV PORT=12121`。

随后同步修改：

- `server.js`
- `env.template`
- `Dockerfile`
- `Dockerfile.alternative`
- `docker-compose.yml`
- `README.md`

#### 验收标准

- 本地运行、独立 Docker 运行和 Compose 运行的监听端口都有明确、正确的文档。
- 容器端口、映射端口和健康检查一致。

---

### API-001：回调响应契约缺少协议依据和测试（优先级待确认）

#### 现状

回调接口部分失败路径返回 JSON，部分路径返回 `failed` 纯文本，成功路径返回解密结果或 `ok`。不同路径使用不同响应并不必然是缺陷，必须先对照企业微信协议确认。

#### 风险

- 当前代码和文档没有明确记录 GET URL 验证与 POST 消息回调所需的精确状态码、`Content-Type`、响应正文、超时和重试语义。
- 后续重构可能误改协议要求。
- 如果现有响应不符合协议，可能导致企业微信重复推送或验证失败。

#### 修复建议

1. 先根据企业微信官方协议列出 GET 验证和 POST 回调每条路径的精确响应要求。
2. 使用固定签名和 XML fixture 建立字节级响应测试。
3. 只有确认当前行为违反协议时，才按 P1 缺陷修复；如果只是内部风格不统一，则降为 P2 可维护性问题。
4. 管理类 API 继续使用明确的 JSON 错误对象，不要为了“统一格式”破坏回调协议。

#### 验收标准

- 每个协议 fixture 都断言明确的状态码、`Content-Type` 和响应正文字节。
- URL 校验成功、签名失败、空消息、解密失败和正常消息确认均有覆盖。
- 文档、测试和实际响应与官方协议一致。

---

## 7. P2：可维护性改进

### TEST-001：缺少自动化测试和 CI

#### 建议优先覆盖

1. `CryptoService` 加密/解密、错误密钥和异常密文。
2. SQLite 新库初始化、CRUD、重复约束和迁移。
3. 普通 API 的必填字段、无效 `code` 和错误码。
4. 企业微信 HTTP API 成功、业务错误码和网络错误。
5. 回调 URL 签名验证、XML 解密和签名失败。
6. 管理接口认证和通知接口限流。
7. 应用启动失败和优雅关闭。

建议使用 mock HTTP server，不要让常规测试依赖真实企业微信凭证。

#### 基础脚本目标

```json
{
  "scripts": {
    "test": "...",
    "lint": "...",
    "format:check": "..."
  }
}
```

CI 至少执行：

```bash
npm ci
npm audit
npm run lint
npm test
```

依赖审计是否阻断 CI 应根据严重级别和临时豁免策略设置，不能永久忽略失败。

---

### ARCH-001：分层和无用代码

#### 现状

- `/api/validate` 在路由层直接调用 `WeChatService`，绕过 service 层。
- `src/api/routes.js` 创建了未使用的 `CryptoService` 实例。
- 数据库初始化作为模块加载副作用发生。

#### 修复建议

- 将凭证验证和成员拉取移入 service 层。
- 删除未使用的实例和 import。
- 通过启动模块进行依赖创建和初始化。
- 保持职责：route 处理 HTTP，service 编排业务，core 处理基础设施。

---

### DEP-002：依赖治理不足

#### 修复建议

- 定期执行 `npm outdated` 和 `npm audit`。
- 对原生模块、加密/XML 库和 HTTP 客户端建立重点升级策略。
- 删除确认未使用的直接依赖。
- 依赖主版本升级单独提交，并包含回归测试结果。
- Docker 中优先使用 `npm ci`，保证严格按锁文件安装。
- 升级到当前受支持的 Node.js LTS，并固定具体补丁版本或镜像 digest，避免基础镜像漂移。

---

### DOC-001：开发和运维文档不足

#### 修复建议

扩充 README，至少包括：

- 本地开发步骤
- 环境变量及安全要求
- 初始化和备份数据库
- 通知 token 与管理权限的区别
- 回调配置流程
- Docker 安全部署要求
- 反向代理、HTTPS 和限流建议
- 密钥备份及轮换限制
- 测试和故障排查命令

`AGENTS.md` 继续用于编码代理和贡献者约定，README 面向使用者和运维人员。

---

## 8. 推荐实施批次

### 第一批：部署面和凭证泄漏止血

建议修改：

- 新增 `.dockerignore`
- 启动时校验 `ENCRYPTION_KEY`
- Compose 改为外部注入密钥
- 配置 TLS 终止，公网仅开放 443，应用端口仅在内部网络可达
- 删除记录完整 `code` 和回调正文的日志
- 移除前端 `@latest`，优先自托管或固定第三方资源版本

验证：

- Docker 构建上下文和镜像不包含密钥、数据库或本机依赖
- 缺少密钥时生产启动正确失败
- HTTP 不处理敏感 API，请求经 HTTPS 到达应用
- 测试日志不包含 token、密钥和消息正文

### 第二批：运行时和依赖安全

建议修改：

- 升级到当前受支持且满足依赖 engine 要求的 Node.js LTS
- 安全升级 `axios`、`express` 等可直接修复依赖
- 移除未使用且存在 Critical 漏洞的 `xmldom`
- 独立评估 `sqlite3` 和 `uuid` 主版本升级
- 增加基础回归测试

验证：

- `node --version` 和依赖 engine 要求一致
- `npm audit`
- Node.js 语法检查
- 本地和 Docker 启动
- SQLite 原生模块和已有数据库兼容性
- 企业微信请求 mock 测试
- 回调 XML 解析测试

### 第三批：权限模型、前端和滥用防护

建议修改：

- 所有配置创建、验证、完善、查询和更新入口增加管理员认证
- 修复 `/api/configure` 重复配置返回已有 `code` 的问题
- 分离通知 token、管理员身份和回调稳定标识
- 增加限流和显式请求体限制
- 将动态前端内容从字符串 `innerHTML` 改为安全 DOM API
- 配置 CSP

验证：

- 使用错误 CorpSecret 重复调用 `/api/configure` 不会得到已有 `code`
- 未认证配置管理访问返回 401/403
- 通知 token 无法读取或修改配置
- 管理员可以正常管理配置
- 超过限流阈值返回 429
- XSS 测试载荷不会执行

### 第四批：启动可靠性、网络和数据库状态

建议修改：

- 修复数据库初始化顺序
- 增加健康检查和优雅关闭
- 为企业微信出站请求增加超时、取消、总截止时间和有限重试
- 重新设计草稿配置状态与唯一约束
- 统一 Docker 端口语义

验证：

- schema 初始化完成前业务端口不对外就绪
- 数据库初始化失败时进程非零退出
- 模拟上游挂起时 API 在截止时间内结束
- 同一 CorpID 的中断重试和并发配置按产品规则工作

### 第五批：敏感字段迁移

建议修改：

- 配置查询不再返回 `callback_token`
- 使用随机化认证加密存储 callback token
- 增加 keyed HMAC/查找摘要以保持等值查询语义
- 为密文增加版本信息
- 设计密钥轮换流程

验证：

- 新旧数据库双读和迁移
- 相同 Token 的重复配置语义保持一致
- 迁移失败可回滚
- 企业微信 URL 校验和回调消息解密正常
- 旧通知配置继续可用

### 第六批：测试、CI 和文档

建议修改：

- 自动化测试
- ESLint/Prettier
- CI
- README 和 API 文档更新
- 根据官方协议固定回调响应 fixture

---

## 9. 每批修复的通用验证清单

1. `npm install` 或 `npm ci` 成功。
2. `npm audit` 结果已记录并解释。
3. 修改过的 JavaScript 文件通过 `node --check`。
4. 服务可以在临时数据库上启动。
5. 数据库初始化、读取、更新和约束行为符合预期。
6. 首页和 API 文档可访问。
7. 普通 API 的成功和错误路径均经过验证。
8. 回调 GET 验证和 POST XML 消息流程经过验证。
9. 日志、响应和构建产物不包含真实凭证。
10. Docker 镜像不包含 `.env`、数据库、`.git` 或本机 `node_modules`。
11. 现有 SQLite 数据升级前有备份，升级失败可回滚。
12. 接口兼容性变化已同步到前端、API 文档和 README。

## 10. 不建议的修复方式

- 不要直接运行 `npm audit fix --force` 后在没有回归测试的情况下上线。
- 不要直接更换已有部署的 `ENCRYPTION_KEY`。
- 不要只依赖 `.gitignore` 保护 Docker 构建上下文。
- 不要继续把 `code` 同时当作通知调用凭证和管理员凭证。
- 不要在响应、日志或前端中回显 CorpSecret、EncodingAESKey、callback token 或完整有效 token。
- 不要在没有迁移方案的情况下直接修改 SQLite 表结构。
- 不要通过关闭依赖审计或忽略全部告警来使 CI 变绿。

## 11. 建议结论

如果项目仅在可信内网短期使用，权限模型整改可以安排迁移窗口，但以下事项仍建议立即处理：

1. 添加 `.dockerignore`。
2. 移除默认生产密钥并增加启动校验。
3. 停止在日志中记录完整 `code` 和回调正文。
4. 修复数据库初始化竞态。
5. 升级受支持的 Node.js LTS，并分析 Critical/High 依赖漏洞。
6. 停止新增任何 callback token 或配置 `code` 的暴露路径。

如果项目计划部署到公网，P0 项应视为上线阻断项。至少在完成 HTTPS、管理接口认证、旧 `/api/configure` 凭证泄漏修复、敏感字段保护、前端 XSS/供应链加固和限流之前，不建议直接暴露服务。
